// Tracey configuration for integration tests

specs (
  {
    name test
    include (spec.md)
    impls (
      {
        name rust
        include (src/**/*.rs)
        test_include (src/tests.rs)
      }
    )
  }

  // Second spec sharing the same marker prefix, for testing multi-spec filtering
  // r[verify ref.prefix.filter+2]
  {
    name other
    include (other-spec.md)
    impls (
      {
        name rust
        // Same source files as test/rust - exercises multi-spec filtering
        include (src/**/*.rs)
      }
    )
  }
)
